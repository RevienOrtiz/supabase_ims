services:
  - type: web
    name: eldera-ims
    env: docker
    dockerfilePath: ELDERA_IMSmodified/ELDERA_IMS/Dockerfile
    dockerContext: ELDERA_IMSmodified/ELDERA_IMS
    autoDeploy: true
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      # Set APP_KEY, DB_* and other secrets in Render dashboard after creation